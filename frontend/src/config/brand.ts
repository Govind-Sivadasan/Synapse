/** Static brand assets in /public (served from site root). */

export const BRAND = {
  /** Main Synapse logo (wordmark) */
  logo: "/synapse.png",
  /** App icon (compact, square) */
  icon: "/synapse.png",
  /** Chatbot assistant avatar */
  chatbot: "/bot_badge.svg",
  chatbotPng: "/bot_badge.png",
  /** Favicon */
  favicon: "/synapse.png",
} as const;
