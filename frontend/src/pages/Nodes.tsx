import PlaceholderPage from "./PlaceholderPage";

export default function Nodes() {
  return (
    <PlaceholderPage
      title="Node Configuration"
      description="CRUD for source and destination PACS nodes — Phase 1 implementation."
    />
  );
}
