import PlaceholderPage from "./PlaceholderPage";

export default function Settings() {
  return (
    <PlaceholderPage
      title="System Configuration"
      description="DIMSE settings, promiscuous mode, retry policy — Phase 1 implementation."
    />
  );
}
