import PlaceholderPage from "./PlaceholderPage";

export default function RoutingMonitor() {
  return (
    <PlaceholderPage
      title="Routing Monitor"
      description="Live routing status with WebSocket updates — Phase 3 implementation."
    />
  );
}
