import { ArrowLeftRight } from "lucide-react";
import PlaceholderPage from "./PlaceholderPage";

export default function MigrationJobs() {
  return (
    <PlaceholderPage
      title="Migration Jobs"
      description="Bulk migration job management with QIDO/WADO retrieval and STOW-RS delivery to cloud PACS."
      icon={ArrowLeftRight}
      phase="Phase 4"
    />
  );
}
