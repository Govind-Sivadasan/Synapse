import PlaceholderPage from "./PlaceholderPage";

export default function RoutingRules() {
  return (
    <PlaceholderPage
      title="Routing Rules"
      description="Conditional routing rule configuration — Phase 1 implementation."
    />
  );
}
